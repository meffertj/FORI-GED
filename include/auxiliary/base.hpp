#ifndef GEDC_BASE_HPP
#define GEDC_BASE_HPP

#include <iostream>
#include <vector>
#include <unordered_map>
#include <algorithm>

/**
 * contains type definitions etc.
 */

using node = uint32_t; // graphs only have hundreds of nodes
using count = uint64_t;
using edge = std::pair<node, node>;
using idx = u_int64_t;


#endif //GEDC_BASE_HPP
